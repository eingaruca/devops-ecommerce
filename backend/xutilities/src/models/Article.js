class Article {
    constructor(
        id,
        title,
        description,
        image,
        article,
        tags
    ) {
        this.id = id,
        this.title = title,
        this.description = description,
        this.image = image,
        this.article = article,
        this.tags = tags;
    }
};

module.exports = Article;